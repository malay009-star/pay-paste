

const PrimaryBtn = ({ text }) => {
  return (
    <div>
      <button className="bg-primary text-white w-full rounded-full py-3">{text}</button>
    </div>
  )
}

export default PrimaryBtn
